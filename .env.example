# Server
PORT=3000
ENFORCE_HTTPS=false
DEFAULT_TENANT_ID=main
HSTS_MAX_AGE_SECONDS=31536000

# Authentication
JWT_SECRET=change-me-in-prod
ACCESS_TOKEN_TTL_SECONDS=900
REFRESH_TOKEN_TTL_SECONDS=604800
PASSWORD_SALT=static-salt
API_KEY=

# PII masking
PII_MASK_LOGS=true
PII_MASK_EXPORTS=false
PII_MASK_REPLACEMENT=*
PII_SENSITIVE_KEYS=email,phone,name,message,subject,address,vin

# Rate limiting
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=300

# Cookies / CSRF
COOKIE_SECURE=false
COOKIE_SAMESITE=lax
COOKIE_DOMAIN=
COOKIE_PATH=/

# Retention
RETENTION_LEADS_DAYS=365
RETENTION_AUDIT_DAYS=90
RETENTION_INTERVAL_HOURS=24
# Rate limiting
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=300
